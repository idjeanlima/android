	<TextView
		android:id="@+id/identificacao"
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		android:text="texto"
		
		android:layout_marginTop="20dp"
		android:layout_gravity="center"
		android:textColor="@color/corPreviamenteDefinida"
		android:textSize="20sp" />
	
	<EditText
		android:id="@+id/entradaId"
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		android:hint="dica"
		
		android:ems="10"
		android:inputType="numberDecimal" />
		
	<Button
		android:id="@+id/btnId"
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		android:text="Botão"
		android:onClick="funcao" />
		
	<CheckBox
		android:id="@+id/boxId"
		android:layout_width="wrap_content"
		android:layout_height="wrap_content"
		android:text="Marcar" />
	
	<RadioGroup
		android:layout_width="wrap_content"
		android:layout_height="wrap_content">
		<RadioButton
			android:id="@+id/radId1"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:text="Radio1" />	
		<RadioButton
			android:id="@+id/radId2"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:text="Radio2" />	
	</RadioGroup>
	
	<Spinner
		android:id="@+id/spinId"
		android:layout_width="150dp"
		android:layout_height="wrap_content"
		android:tooltipText="Spinner" />
